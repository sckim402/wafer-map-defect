# W9 — 11월 목록 #5: 방위 기각률의 **lot 블록 부트스트랩** (사전 등록)

> 🔴 **§1~§3은 결과를 보기 전에 쓰고 실행 전에 커밋한다.** §4만 나중에 채운다.

---

## 1. 무엇을 재는가 — ⛔ **세 가지를 섞지 않는다**

D-036이 *"몬테카를로 폭·웨이퍼 표본 오차·lot 군집 오차는 **다른 양이라 섞지 않는다**"*고
못박았다. 셋 중 **무엇이 미측정인지**부터 적는다.

| # | 불확실성 | 상태 |
|---|---|---|
| ① | **몬테카를로 폭** (맵 안의 순열 `B=399`) | ✅ 측정됨 — 귀무 seed만 바꿔도 **≤1.5%p** (D-034, 7차 A3) |
| ② | **차이**(`Edge-Loc` − `Loc`)의 lot 구간 | ✅ 측정됨 — 같은 `n`·`k` **+40.1%p [36.7, 43.6]** (D-036, 8차) |
| ③ | **개별 클래스 기각률의 표본 + lot 군집 오차** | ⬜ **미측정 — 이것이 #5다** |

**목적은 하나다**: `CLAUDE.md` §7의
*"❌ 방위 기각률을 소수점 첫째 자리까지 인용 — 표본 불확실성은 아직 안 쟀다. **방향만 쓴다**"*
**이 금지를 풀 수 있는가, 아니면 유지해야 하는가.**

## 2. 방법

- **전수**로 간다. `Edge-Loc` 5,189 · `Loc` 3,593 · `Edge-Ring` 9,680 · `Center` 4,294 = **22,756장**.
  현행 표(72.2 / 19.0 / 25.1 / 7.8%)는 **클래스당 400장 표본**이다.
- ⛔ **`one_map`의 RNG 소비 패턴을 바꾸지 않는다.** D-034가 고친 자리이고, 벡터화하면
  순열이 통째로 달라져 **기존 수치와 비교 불가**가 된다. `null_rng(ci, i, nseed)` 규약 그대로.
- 맵별 기각 지시자(`p ≤ 0.05`)와 **lot**을 함께 모은 뒤, **lot을 블록으로 재추출**
  (`B_boot=2,000`, D-029·D-048과 같은 꼴). 백분위 95% 구간.
- **대조로 Wilson 구간**(독립 가정)을 같이 낸다. **설계 효과** `deff = (lot폭 / Wilson폭)²`.

## 3. 사전 등록 — 예측과 반증 조건 (**양방향으로 쓴다**)

> 🔴 **D-049에서 걸린 것을 반영한다.** #8의 반증 조건은 **한 방향만** 막아서,
> 실제로 일어난 반대쪽이 가설을 위협하지 않는데도 「어긋남」으로 기록됐다.
> **여기서는 각 예측마다 「반대로 나오면 그것이 무엇을 뜻하는지」를 미리 적는다.**

| # | 예측 | **맞으면** | **틀리면 (반대 방향)** |
|---|---|---|---|
| **H1** | `deff > 1` (lot 군집이 구간을 **넓힌다**) | **Wilson으로 인용하면 과신**이다. lot 구간을 쓴다 | `deff ≤ 1`이면 **lot 군집이 기각률에 안 걸린다**. 그것도 결과이고 **인용에는 오히려 유리**하다 — **H1의 방향은 목적을 위협하지 않는다** |
| **H2** | `Edge-Loc` 하한 **>** `Loc` 상한 | *"기각률은 더 높았다"*(초록 v9)가 표본 축에서도 선다 | **겹치면 그 문장이 흔들린다.** ⚠ **이것만은 한 방향이 맞다** — 겹침은 순수한 나쁜 소식이다 |
| **H3** | 전수 기각률이 400장 표본값의 Wilson 구간 **안** | 400장 표본에 대표성이 있었다 | **밖이면 400장 표본이 치우쳤다**는 뜻. 그러면 **현행 표의 값 자체를 못 쓴다** |

**⛔ 「몇 %p 이하면 인용 가능」은 정하지 않는다** (§3-13). **구간을 내고 그 폭을 보고한다.**
판단(*"이 폭이면 소수점을 쓸 수 있는가"*)은 구간을 본 뒤 **별도 항목으로** 기록한다.

---

## 4. 결과 (2026-09-16 · `src/w9_azimuth_boot.py`)

**§1~§3은 고치지 않았다.** 등록 커밋 `8f94496`(20:25:38)이 실행보다 앞선다.

### 4-A. 전수 22,264장 (제외 492장)

| 클래스 | 전수 | 제외 | **lot** | lot당 | **전수 기각률** | Wilson 95%(독립) | **lot 블록 95%** | **deff** |
|---|---|---|---|---|---|---|---|---|
| `Edge-Loc` | 5,187 | 2 | 3,210 | 1.6 | **71.70%** | [70.46, 72.91] ±1.23 | **[70.17, 73.16] ±1.49** | **1.49** |
| `Loc` | 3,373 | 220 | 2,417 | 1.4 | **20.07%** | [18.75, 21.46] ±1.35 | **[18.62, 21.56] ±1.47** | **1.18** |
| `Edge-Ring` | 9,411 | 269 | 1,065 | **8.8** | **24.95%** | [24.09, 25.83] ±0.87 | **[21.46, 28.50] ±3.52** | **16.20** |
| `Center` | 4,293 | 1 | 1,647 | 2.6 | **9.67%** | [8.82, 10.59] ±0.88 | **[8.40, 10.95] ±1.27** | **2.07** |

### 4-B. 사전 등록 판정

| | 결과 |
|---|---|
| **H1** `deff > 1` | ✅ **네 클래스 전부** (1.18 ~ **16.20**) |
| **H2** `Edge-Loc` 하한 > `Loc` 상한 | ✅ **70.17% > 21.56%** — 분리. 초록 v9 문장이 표본 축에서도 선다 |
| **H3** ~~400장 표본값이 전수 구간 안~~ | ✅ **네 클래스 전부 통과** — **등록 방향**(전수값 ∈ 400장 표본의 Wilson 구간) · **실제 k/n**(§8). ⛔ **정정 (10차→11차→12차)**: 원래 ~~「❌ `Center`만 밖이다 — 400장 7.8% vs 전수 구간 [8.40, 10.95]」~~였다. **코드가 등록문과 반대 방향을 쟀다** |

### 4-C. ★ `Edge-Ring`의 `deff = 16.20` — 이미 아는 구조다

`Edge-Ring`은 **lot 1,065개에 9,411장 = lot당 8.8장**이다. 다른 셋은 1.4~2.6장.
**같은 lot에 Edge-Ring이 무더기로 들어 있다.**

> 🟢 **이것은 §7의 누수 수치와 같은 구조다** — *"웨이퍼 단위 random split이면
> 검증 웨이퍼의 79.5%(**`Edge-Ring`은 97.0%**)가 같은 lot의 웨이퍼를 학습에 갖는다"*.
> **분할 정책(D-003)을 lot 단위로 바꾸게 한 그 군집이, 방위 기각률의 구간에서도 그대로 나온다.**
> **독립 가정(Wilson)을 쓰면 `Edge-Ring`에서 폭을 4배 과소평가한다**(±0.87 → ±3.52%p).
>
> ⚠ **(12차에 덧붙임)** 여기서 「같은 구조」는 **lot당 장수**(클러스터 크기)까지다 — 클러스터가 크면
> **같은 lot 내 상관에서도** 설계 효과가 커진다. **기각 지시자의 lot 내 상관 자체는 따로 재지 않았고**,
> 라벨 군집(누수 97.0%·D-052의 `R`)으로 이 `deff`를 **설명하지 않는다**(D-053 — 결과 변수가 다르다).

### 4-D. `Center` — ~~400장 표본이 치우쳤다~~ → 전수값을 쓴다

> ⛔ **정정 (10차 → §6·§7·§8)**: 원래 제목은 「400장 표본이 치우쳤다」였다. **철회한다** —
> 그 근거는 **등록문과 반대 방향으로 구현한 H3**였고, 등록 방향으로 재면 `Center`도 **통과**한다.
> **아래 「밖이다」는 값 자체로는 참**이지만 **표본 편향의 증거가 아니다.**

**전수 9.67%**인데 400장 표본은 **7.8%**(31/400)였고, 전수 lot 구간 **[8.40, 10.95] 밖**이다.
⛔ **현행 표의 `Center` 7.8%를 인용하지 않는다** — 이유는 **「전수가 표본보다 낫다」**이지 표본이 치우쳐서가 아니다.

> 🟢 **§7의 기존 판정은 더 강해진다.** *"`Center` 8.2%는 자연 귀무 기준선"*을 금지하며
> *"공칭 5%에서 33/400은 이항 상단 밖"*이라고 적어 뒀는데, **전수는 9.67% [8.40, 10.95]**로
> **5%에서 훨씬 더 멀다.** `Center`도 층 내 균등 재배치보다 기각이 많다.

### 4-E. 판정 — **금지는 유지한다. 단 이유가 바뀐다**

`CLAUDE.md` §7: *"방위 기각률을 소수점 첫째 자리까지 인용 — **표본 불확실성은 아직 안 쟀다**"*.

**이제 쟀다. 그리고 소수점은 여전히 못 쓴다** — 이유가 *"안 쟀다"*에서 ***"쟀더니 폭이
`Edge-Loc` ±1.5%p · `Edge-Ring` ±3.5%p"***로 바뀐다. **여기에 몬테카를로 폭 ≤1.5%p가 더 얹힌다**
(D-036: 다른 양이므로 섞지 않는다 = **따로 더한다**).

> ✅ **바뀌는 것**: *"방향만 쓴다"* → **"구간으로 쓸 수 있다."**
> `Edge-Loc` **71.7% [70.2, 73.2]** · `Loc` **20.1% [18.6, 21.6]** ·
> `Edge-Ring` **25.0% [21.5, 28.5]** · `Center` **9.7% [8.4, 11.0]** (전수 · lot 블록 95%).
> ⛔ **점추정을 소수점 첫째 자리로 단독 인용하는 것은 계속 금지.**

### 4-F. ⚠ 다루지 않은 것

- **제외율이 클래스마다 다르다**: `Loc` **6.1%**(220/3,593 — ⛔ **정정 (10차)**: 옛 «6.5%»는 220/3,373으로 **분모가 적격 수였다**) · `Edge-Ring` 2.8% ·
  `Edge-Loc` 0.04% · `Center` 0.02%. 제외는 최외곽 층이 작거나(`MIN_DIE`) 불량이 3 미만이거나
  전부 불량이라 **검정이 퇴화**하는 경우다. **기각률은 「방위를 잴 수 있는 웨이퍼」 위의 조건부 값**이고,
  `Loc`에서 그 조건이 가장 자주 깨진다. **이 편향은 안 쟀다.**
- **몬테카를로 폭과 합성하지 않았다** (D-036의 금지). 두 폭을 더하려면 별도 설계가 필요하다.
- 맵 안 순열은 `B=399`, `nseed=0` **하나**다.


---

## 6. 🔴 10차 외부 검토 정정 (2026-09-17) — **H3을 뒤집어 구현했다**

**등록문 §3의 H3**: *"**전수 기각률이 400장 표본값의 Wilson 구간 안**"* — 대상은 **전수값**,
분산은 **400장 표본**의 것이다.

**코드(`w9_azimuth_boot.py`)가 한 것**: *"400장 표본값이 **전수값의 lot 블록 구간** 안"* —
**대상과 분산이 둘 다 뒤바뀌었다.** 표본 크기도 다르다.

**등록한 방향으로 다시 계산했다** (400장·기각 수는 보고된 비율에서 복원):

> ⛔ **정정 (12차 → §8)**: 아래 표의 `Loc`·`Edge-Ring` 분모 **400은 틀렸다** — 실제는 **374 · 387**이고
> 구간은 **[15.33, 23.27] · [21.01, 29.61]**이다. **실제 k/n은 처음부터 기록에 있었다.** 판정(안)은 같다.

| 클래스 | 400장 | Wilson 95% | 전수 | 등록된 H3 |
|---|---|---|---|---|
| `Edge-Loc` | 72.25% | [67.67, 76.41] | 71.70% | ✅ 안 |
| `Loc` | ~~19.00%~~ **71/374 = 18.98%** | ~~[15.46, 23.13]~~ **[15.33, 23.27]** | 20.07% | ✅ 안 |
| `Edge-Ring` | ~~25.00%~~ **97/387 = 25.06%** | ~~[21.01, 29.47]~~ **[21.01, 29.61]** | 24.95% | ✅ 안 |
| **`Center`** | **7.75%** | **[5.51, 10.79]** | **9.67%** | ✅ **안** |

🔴 **등록한 대로 재면 네 클래스 전부 통과한다. `Center`도 통과한다.**
⛔ **따라서 *"400장 표본이 치우쳐 있었다(H3 기각)"*를 철회한다.** ~~판정은 **보류**다.~~ → **통과** (11차 §7 · 12차 §8)
~~⚠ **400장 표본의 실제 분모·기각 수 기록이 없어** 정확한 복원이 아니다 — 그래서 *"통과"*가 아니라
***"기각 근거가 없다"***가 정확하다.~~ (⛔ **12차: 전제가 거짓이다 — 기록은 있었다.** §8)
**95% 구간 밖이라는 사실만으로 체계적 편향이 증명되지도 않는다.**

**남는 것**: **전수값을 쓰는 이유는 여전히 유효하다**(전수가 표본보다 낫다). 바뀐 것은
*"400장이 치우쳤다"*는 **추가 주장**이고, 그 주장은 **등록한 검사가 지지하지 않는다.**

⚠ **불확실성 합성 (10차 M15)**: 본문의 *"lot 구간과 몬테카를로 폭은 다른 양이라 섞지 않는다"*와
*"여기에 ≤1.5%p가 따로 더해진다"*는 **서로 충돌한다.** 둘은 같은 신뢰수준의 오차항이 아니다.
✅ **고정 `n_seed`에서의 lot 구간**과 **별도의 MC 민감도**로 **각각** 보고한다.

---

## 7. 🔴 11차 외부 검토 정정 (2026-09-17) — **H3의 「보류」는 과소서술이었다**

§6에서 *"400장 표본의 실제 분모·기각 수 기록이 없어 정확한 복원이 아니다 —
그래서 「통과」가 아니라 「기각 근거가 없다」가 정확하다"*고 적고 **판정을 보류**로 뒀다.
**그 유보가 불필요했다.** 검토자가 **분모를 모르는 채로도 판정이 서는지**를 물었고,
**보고된 비율의 반올림과 양립하는 모든 `(n, k)`**를 전수로 확인했다. 내가 재현했다:

| 클래스 | 양립하는 `(n,k)` | **전수값을 못 품는 경우** | 모든 구간의 교집합 |
|---|---:|---:|---|
| `Edge-Loc` | 82 | **0건** | [67.67, 76.34] ∋ 71.70% |
| `Loc` | 88 | **0건** | [15.50, 23.13] ∋ 20.07% |
| `Edge-Ring` | 76 | **0건** | [21.12, 29.54] ∋ 24.95% |
| `Center` | 83 | **0건** | [5.58, 10.79] ∋ **9.67%** |

**329개 조합 중 판정이 뒤집히는 것이 0건**이다 — `1 ≤ n ≤ 400`의 **어떤 분모에서도** 전수값이
Wilson 구간 안이다. **모르는 값이 판정을 좌우하지 않으므로 유보할 이유가 없다.**

> ✅ **H3의 판정을 「보류」에서 「통과 — 네 클래스 전부」로 올린다.**
> ⛔ **단 이것이 「400장 표본이 대표성 있다」는 뜻은 아니다.** 등록된 조건은
> **한 번의 구간 포함**이고, 그것은 무편향의 증명이 아니다.
> ⛔ 전수값을 쓰는 이유는 **여전히 「전수가 표본보다 낫다」**이지 표본이 나빠서가 아니다.

### 7-A. 내가 왜 이것을 과소서술했나 — **10차의 방향과 반대다**

10차에서 내 자기진단 셋이 전부 **「덜함」**(통과한 것을 기각으로, 충족한 것을 불충족으로)
쪽으로 틀렸고, **그것을 고치는 §6에서도 한 칸을 덜 갔다.**
*"기록이 없다"*를 곧바로 *"판정할 수 없다"*로 옮긴 것인데,
**모르는 값이 결론을 바꾸는지 먼저 확인했어야 했다** — 확인해 보니 안 바꾼다.

⛔ **「자료가 없다」는 「판정 불가」의 충분조건이 아니다. 없는 값의 가능한 범위 전체에서
판정이 같으면 판정은 선다.** ✅ 반대로 **범위 안에서 갈리면** 그때가 진짜 `⬜`다.

---

## 8. 🔴 12차 외부 검토 반영 (2026-09-17) — **「기록이 없다」는 거짓이었다. 그리고 코드는 아직 반대 방향을 인쇄했다**

### 8-A. 400장 표본의 실제 k/n은 **9/12부터 리포에 있었다**

§6·§7은 둘 다 *"400장 표본의 실제 분모·기각 수 기록이 없다"*를 전제로 썼다. **틀렸다.**
`docs/pattern_process_mapping.md`(D-034, 커밋 `e53721d`)의 표에 **그대로 있다**:
`Edge-Loc` **289/400** · `Loc` **71/374** · `Edge-Ring` **97/387** · `Center` **31/400**.
`reproduction.md`에도 *"먼저 400장을 뽑고 그 다음 제외한다. 분모는 적격 표본 수"*라고 적혀 있다.

**실제 k/n으로 등록 방향 H3를 다시 냈다** (전수 재실행, 2026-09-17 · 수치는 D-050과 자릿수까지 동일):

```
H3 전수 기각률이 400장 표본의 Wilson 구간 안인가 (등록 방향 · 실제 k/n):
   Edge-Loc    전수 71.70%  vs 400장 289/400 Wilson [67.67%, 76.41%]  → ✅ 안
   Loc         전수 20.07%  vs 400장 71/374 Wilson [15.33%, 23.27%]  → ✅ 안
   Edge-Ring   전수 24.95%  vs 400장 97/387 Wilson [21.01%, 29.61%]  → ✅ 안
   Center      전수  9.67%  vs 400장 31/400 Wilson [5.51%, 10.79%]  → ✅ 안
```

✅ **판정은 §7과 같다 — 네 클래스 전부 통과.** 바뀐 것은 **근거**다:
§7의 *"329개 양립 조합 전부에서 불변"*은 **보강 논증**으로 남고, **주 근거는 실제 기록**이다.
⛔ §6 표의 `Loc`·`Edge-Ring` 행(분모 400 가정)을 **「실제 표본의 복원」으로 인용하지 않는다.**
⛔ 「안」은 **한 번의 구간 포함**이다 — 표본의 대표성·무편향을 증명하지 않는다.

> 🔴 **§7-A의 교훈이 한 겹 더 깊었다.** 11차는 *"자료가 없다 → 판정 불가"*의 **추론**을 고쳤는데
> **「자료가 없다」는 전제 자체를 검색하지 않았다.** 내 리포에서 `289 / 400`을 grep 한 번 했으면 끝났다.
> **「기록이 없다」는 주장이다 — 쓰기 전에 자기 기록을 검색한다** (D-055).

### 8-B. 코드가 **여전히 옛 방향을 인쇄하고 있었다**

10차에 §6을 쓰고 11차에 §7을 쓰는 동안 **`src/w9_azimuth_boot.py`의 H3 블록은 그대로**였다 —
*"400장 표본값이 전수 기각률의 구간 안인가"* · `Center` **`❌ 밖`**. **다시 돌리면 철회한 판정이 다시 나온다.**
→ **`h3()`를 등록 방향으로 새로 쓰고**, `SAMPLE400`에 실제 k/n을 박았다.
`demo()`에 **두 방향이 실제로 갈리는 `Center` 사례**를 넣어 방향이 뒤집히면 실패하게 했다.

### 8-C. 원위치에서 고친 것

§4-B H3 행(❌ → ✅) · §4-D 제목(「치우쳤다」 철회) · §4-C 「같은 구조」 범위(`deff`를 라벨 군집으로 설명하지 않는다) ·
§6 표의 분모 · §6 「보류」·「기록이 없어」. 경위는 **D-055**.
